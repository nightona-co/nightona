---
title: Regions
description: Choose specific geographic locations for reduced latency, comply with data residency requirements, or use your own runner machines for custom regions.
---

import { TabItem, Tabs } from '@astrojs/starlight/components'

Sandboxes are isolated runtime environments that run on [runners](/docs/en/bring-your-own-compute) — machines that form Nightona's compute plane.

Runners are organized into **regions**, which are geographic or logical groupings of compute infrastructure. When creating a sandbox, you can target a specific region, and Nightona will schedule your workload on an available runner within that region.

As a result, you're able to:

- Choose specific geographic locations for reduced latency
- Comply with data residency requirements
- Use your own runner machines for custom regions
- Scale compute resources independently within each custom region

Regions are geographic or logical groupings of runners that execute sandbox workloads. The sandbox region is specified by setting the `target` parameter on initialization:

<Tabs syncKey="language">
<TabItem label="Python" icon="seti:python">

```python
from nightona import Nightona, NightonaConfig

# Configure Nightona to use the US region
config = NightonaConfig(
    target="us"
)

# Initialize the Nightona client with the specified configuration
nightona = Nightona(config)
```
</TabItem>
<TabItem label="TypeScript" icon="seti:typescript">
```typescript
import { Nightona } from '@nightona/sdk';

// Configure Nightona to use the EU region
const nightona: Nightona = new Nightona({
    target: "eu"
});
```

</TabItem>
<TabItem label="Go" icon="seti:go">

```go
package main

import (
    "github.com/nightona-co/nightona/libs/sdk-go/pkg/nightona"
    "github.com/nightona-co/nightona/libs/sdk-go/pkg/types"
)

// Configure Nightona to use the US region
client, _ := nightona.NewClientWithConfig(&types.NightonaConfig{
    Target: "us",
})
```

</TabItem>
<TabItem label="Ruby" icon="seti:ruby">

```ruby
require 'nightona'

# Configure Nightona to use the EU region
config = Nightona::Config.new(
    target: "eu"
)

# Initialize the Nightona client with the specified configuration
nightona = Nightona::Nightona.new(config)
```

</TabItem>
<TabItem label="Java" icon="seti:java">

```java
import io.nightona.sdk.Nightona;
import io.nightona.sdk.NightonaConfig;

// Configure Nightona to use the US region
NightonaConfig config = new NightonaConfig.Builder()
        .apiKey(System.getenv("NIGHTONA_API_KEY"))
        .target("us")
        .build();

Nightona nightona = new Nightona(config);
```

</TabItem>
</Tabs>

### Shared regions

Shared regions are managed by Nightona and available to all organizations. These regions provide immediate access to Nightona's infrastructure without any setup required.

Limits are applied to your organization's default region. For access to a different shared region, contact [sales@daytona.io](mailto:sales@daytona.io).

| Region        | Target   |
| ------------- | -------- |
| United States | **`us`** |
| Europe        | **`eu`** |

### Dedicated regions

Dedicated regions are managed by Nightona and provisioned exclusively for individual organizations. These regions deliver dedicated infrastructure with the operational simplicity of a managed service.

:::note
Contact [sales@daytona.io](mailto:sales@daytona.io) to set up a dedicated region for your organization.
:::

### Custom regions

Custom regions are created and managed by your organization, allowing you to use your own runner machines and scale compute resources independently within each region. This provides maximum control over data locality, compliance, and infrastructure configuration.

Additionally, custom regions have no limits applied for concurrent resource usage, giving you full control over capacity and performance.

For more information, see the [bring your own compute (BYOC)](/docs/en/bring-your-own-compute) guide.
